/**
 * Cross domain
 *
 * @see ./utils.js & ./message.php
 */

// Set as false to skip XHR tests
window.CORS = false

// Or use the server alias or its IP
// window.CORS  = 'http://127.0.0.1/test'
// window.CORS  = 'http://test.localhost/test'

/**
 * Share endpoint
 *
 * @see ./discreto.js
 */

// Set as default discre.to endpoint
window.SHARE = 'https://cookie.discre.to'
// Or use your custom cookie repeater
// window.SHARE = 'https://localhost/cookie'
